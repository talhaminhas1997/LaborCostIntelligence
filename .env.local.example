# Copy this file to .env.local and add your key.
# The app runs fine WITHOUT a key (it falls back to a deterministic local analysis),
# but a real key produces genuine AI-reasoned benchmarks.
ANTHROPIC_API_KEY=sk-ant-...
